syntax = "proto3";

package definitions.demo;

service DemoService {
  rpc IsTrue (DemoRequest) returns (DemoResponse);
}

message DemoRequest {
  int32 value = 1;
}

message DemoResponse {
  bool is_true = 1;
}
